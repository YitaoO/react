import Taro, { Component } from "@tarojs/taro";
import { connect } from "@tarojs/redux";
import { View, Text } from "@tarojs/components";

import "./index.scss";

@connect(({ userInfo, car }) => ({
  userInfo,
  car
}))
export default class HistoryIndex extends Component {
  config = {
    navigationBarTitleText: "历史"
  };
  constructor(props) {
    super(props);
    this.state = {};
  }
  componentDidMount() {
    this.props.dispatch({
      type: "car/getCarTree",
      payload: {
        UserId: this.props.userInfo.carUserInfo.UserID
      }
    });
  }

  render() {
    return (
      <View className="history-index-wrap">
        <View className="index-top">
          <View className="top-wrap">
            <View className="left">
              <View className="title">高恩路面</View>
              <View className="value">粤ABE391</View>
            </View>

            <View className="right">选择车辆</View>
          </View>
        </View>
        {/* <View className="index-center">
          <View className="title">历史轨迹</View>
          <View className="lists">
            <View className="item">
              <View className="left" />
              <View className="right" />
            </View>
          </View>
        </View> */}
      </View>
    );
  }
}
